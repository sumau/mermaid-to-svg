# No-block example

This page is Markdown but contains no ` ```mermaid ` block, so there is nothing
to convert. By design the workflow skips it and emits a warning rather than
failing the run — no SVG is produced for this source.
